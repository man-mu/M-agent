VITE_BASE_URL=
VITE_BACKEND_PROXY=http://localhost:18080

# 同源部署时保持 VITE_BASE_URL 为空，由反向代理或同源后端处理 /api 与 /chat。
# 前后端分离部署时设置 VITE_BASE_URL=http://host:18080。
# 本地开发代理目标使用 VITE_BACKEND_PROXY，未设置时默认 http://localhost:18080。
