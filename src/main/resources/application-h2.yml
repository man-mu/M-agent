spring:
  datasource:
    url: jdbc:h2:mem:manmu_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  r2dbc:
    url: r2dbc:h2:mem:///manmu_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
  flyway:
    url: jdbc:h2:mem:manmu_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    user: sa
    password:
    locations: classpath:db/h2-migration

mvp:
  rag:
    enabled: false
  mcp:
    enabled: true
